# Jubayer Mumin portfolio — clean URL version

This version uses folder-based clean URLs for GitHub Pages/static hosting:

- /
- /publications/
- /leadership/
- /development/
- /honors/
- /contact/

The old spa-router.js script has been removed from page loads so each page renders consistently after navigation and refresh.

To deploy on GitHub Pages, upload the contents of this folder to the root of your repository.
